<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<!-- MENU 테이블에 전달되는 값을 INSERT하는 구문 -->
	<entry key="insertMenu">
		INSERT
		  INTO TBL_MENU
		(
		  MENU_CODE
		, MENU_NAME
		, MENU_PRICE
		, CATEGORY_CODE
		, ORDERABLE_STATUS
		)
		VALUES
		(
		  SEQ_MENU_CODE.NEXTVAL
		, ?
		, ?
		, ?
		, ?
		)
	</entry>
	<entry key="updateMenu">
		UPDATE
		       TBL_MENU
		   SET MENU_NAME = ?
		     , MENU_PRICE = ?
		     , CATEGORY_CODE = ?
		     , ORDERABLE_STATUS = ?
		 WHERE MENU_CODE = ?
	</entry>
	<entry key="deleteMenu">
		DELETE
		  FROM TBL_MENU 
		 WHERE MENU_CODE = ?
	</entry>
</properties>












